import React from 'react';
import ReactDOM from 'react-dom';

const rootElement = document.getElementById('root');
const sampleComponent = <div>React Starter!</div>;

ReactDOM.render(sampleComponent, rootElement);
