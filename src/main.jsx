import React from 'react';
import ReactDOM from 'react-dom';

const rootElement = document.getElementById('root');
const sampleComponent = <div>React working!</div>;

ReactDOM.render(sampleComponent, rootElement);
